{
    "$schema": "https://apify.com/schemas/v1/output.ide.json",
    "actorOutputSchemaVersion": 1,
    "title": "Output schema",
    "properties": {
        "results": {
            "type": "string",
            "title": "Results",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        },
        "allRuns": {
            "type": "string",
            "title": "All Runs",
            "template": "{{storages.datasets.allRuns.apiUrl}}/items"
        },
        "chart": {
            "type": "string",
            "title": "Chart",
            "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/chart"
        }
    }
}
